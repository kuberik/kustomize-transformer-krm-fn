name: Publish

on:
  pull_request:
  push:

permissions:
  contents: write
  packages: write

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v4
        with:
          go-version: "1.23.x"
      - uses: actions/checkout@v3

      - uses: ko-build/setup-ko@v0.6

      - id: get_tag
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            echo "tag=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - run: KO_DOCKER_REPO=ghcr.io/kuberik/kpt-fn/kustomize-transformer-generator ko build --bare ./functions/kustomize-transformer-generator --tags latest,${{ steps.get_tag.outputs.tag }}
      - run: KO_DOCKER_REPO=ghcr.io/kuberik/kpt-fn/kustomize-transformer ko build --bare ./functions/kustomize-transformer --tags latest,${{ steps.get_tag.outputs.tag }}
