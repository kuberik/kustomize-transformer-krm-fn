apiVersion: v1
kind: ConfigMap
metadata:
  name: transform
  annotations:
    config.kubernetes.io/function: |
      container:
        image: ubuntu
        network: false
        mounts:
          - src: .
            dst: /work
            type: bind
